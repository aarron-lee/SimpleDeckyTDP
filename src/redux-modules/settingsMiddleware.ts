import { Action, Dispatch } from "redux";
import { RootState } from "./store";

export const settingsMiddleware =
  (store: RootState) => (dispatch: Dispatch) => (action: Action<any>) => {
    //
  };
